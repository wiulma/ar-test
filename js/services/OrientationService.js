const orientationService = {
    init () {
        console.log("init orientation service");
    },

    debugger() {
        const c = document.createDocumentFragment();
        
        document.appendChild(c);
    }
} 